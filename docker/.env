# Personalizzazione container e immagine
CONTAINER_NAME=tonappa_ros_project
TAG=dynamic
PROJECT_NAME=test_dockercompose

# Variabili di sistema (di solito non serve modificare)
#XAUTH=/tmp/.docker.xauth
DISPLAY=${DISPLAY}
XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR}